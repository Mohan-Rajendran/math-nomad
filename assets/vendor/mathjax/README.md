# MathJax browser assets

This directory contains the MathJax 3.2.2 `tex-chtml-full` browser bundle and its CHTML webfonts. They are served locally so mathematical pages do not depend on a third-party CDN at runtime.

MathJax is distributed under the Apache License 2.0. The upstream licence is included in `LICENSE`.
